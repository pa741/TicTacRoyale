import styles from '../../styles/game.module.css'
import React from 'react';
function Draw() {
    return (
        <div>
            <div className={`${styles.draw}`}>
                <h1>Draw</h1>
            </div>
        </div>

    );
}
export default Draw;